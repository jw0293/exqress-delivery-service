server:
  port: 4001
  url: http://k8s-default-backdeli-ecf39846be-205392414.ap-northeast-2.elb.amazonaws.com/

spring:
  application:
    name: capstone
  redis:
    port: 6379
    host: http://my-redis.redis-ns.svc.cluaster.local
#  profiles:
#    active: local
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
      path: /h2-console
  datasource:
    #url: jdbc:h2:mem:testdb
    url: jdbc:mysql://exqress-rds.ctxktr2nwban.ap-northeast-2.rds.amazonaws.com:3306/test/useSSL=false&useUnicode=true&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: admin
    password: qwer1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
#    generate-ddl: true
#    database: h2
#    defer-datasource-initialization: true

#eureka:
#  instance:
#    instance-id: ${spring.cloud.client.hostname}:${spring.application.instance_id:${random.value}}
#  client:
#    register-with-eureka: true
#    fetch-registry: true
#    service-url:
#      defaultZone: http://127.0.0.1:8761/eureka


springdoc:
  swagger-ui:
    path: /swagger-ui.html
    groups-order: DESC
    operationsSorter: method
    disable-swagger-default-url: true
    display-request-duration: true
  api-docs:
    path: /api-docs
  show-actuator: true
  default-consumes-media-type: application/json
  default-produces-media-type: application/json
  paths-to-match:
    - /**

management:
  endpoints:
    web:
      exposure:
        include: "*"

token:
  access_expiration_time: 864000
  refresh_expiration_time: 8640000
  secret: exqress-token